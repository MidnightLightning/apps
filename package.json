{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "dependencies": {
  },
	"scripts": {
		"build:gopkg": "export APPS_PATH_ABS=$(pwd) && lerna run clean && lerna run build:gopkg",
		"compile": "lerna run compile",
    "clean": "lerna run clean",
		"size": "lerna run size"
	}
}
